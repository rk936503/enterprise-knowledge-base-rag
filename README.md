# Enterprise Knowledge Base API

A multi-tenant Retrieval-Augmented Generation (RAG) platform. Organizations upload
documents and query them via API for grounded, cited answers.

**Status:** Phase 1 in progress — core single-tenant RAG pipeline.

## Stack
FastAPI, PostgreSQL + pgvector, Docker, OpenAI embeddings, Anthropic Claude for generation.